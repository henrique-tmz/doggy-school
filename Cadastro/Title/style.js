import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  boxTitle: {
    padding: 15,
    width: '70%',
  },
  textTitle: {
    color: '#000000',
    fontSize: 45,
    fontWeight: 'Regular',
  },
});

export default styles;