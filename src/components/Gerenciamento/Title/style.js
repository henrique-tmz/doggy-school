import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  boxTitle: {
    marginLeft: 15,
    width: '70%',
    height: 70,
  },
  textTitle: {
    color: '#000000',
    fontSize: 42,
    fontWeight: 'Regular',
  },
});

export default styles;
