import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  boxPata: {
    alignItems: 'colum',
    height: 64,
    width: 90,
  },
  pataImg: {
    height: 64,
    width: 64,
  },
});

export default styles;